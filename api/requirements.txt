# FastAPI and related
fastapi>=0.104.0
python-multipart>=0.0.6

# Pydantic and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0  # PostgreSQL driver for Supabase

# Serverless adapter for FastAPI
mangum>=0.17.0

# Utilities
python-jose[cryptography]>=3.3.0
authlib>=1.2.0
httpx-oauth>=0.11.0

# Environment and config
python-dotenv>=1.0.0

# HTTP client
httpx>=0.25.0
requests>=2.31.0

# Note: Camera processing dependencies (opencv-python, ultralytics, numpy, pillow)
# are excluded due to Vercel function size limits. If camera features are needed,
# consider using a separate service or Vercel Edge Functions.

